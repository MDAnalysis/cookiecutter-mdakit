name: mdakit-Cookie-test
channels:

dependencies:
  # Base depends
  - python
  - pip

  # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov

  # Pip-only installs
  #- pip:
  #  - codecov
