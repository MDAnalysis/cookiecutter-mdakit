name: {{cookiecutter.repo_name}}-test
channels:
{% if cookiecutter.__dependency_source == 'conda-forge' %}
  - conda-forge
{% endif %}
dependencies:
  # Base depends
  - python
  - pip
{% if cookiecutter.__dependency_source == 'conda-forge' %}
  # MDAnalysis
  - mdanalysis
{% endif %}
  # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov

  # Pip-only installs
  #- pip:
  #  - codecov
